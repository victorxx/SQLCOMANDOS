CREATE TABLE DADO(
    id SERIAL PRIMARY KEY,
    nome VARCHAR(50),
    saldo DECIMAL(10,2)
);
INSERT INTO DADO (nome, saldo) VALUES
('Joao', 1200.00),
('Maria', 800.00),
('Carlos', 1500.00);
BEGIN;
SELECT * FROM DADO WHERE SALDO>100
FOR UPDATE;
UPDATE DADO SET SALDO=SALDO-100
WHERE SALDO>100;
COMMIT;


