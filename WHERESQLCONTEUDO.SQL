CREATE TABLE IF NOT EXISTS USUARIO(
ID INT PRIMARY KEY auto_increment,
LOGIN VARCHAR(300),
SENHA VARCHAR(300)
);
INSERT INTO USUARIO(LOGIN,SENHA)VALUES('VICTOR','123');
CREATE TABLE IF NOT EXISTS CONTEUDO(
ID INT PRIMARY KEY auto_increment,
TEXTO TEXT,
ID_USER INT,
foreign key(ID_USER)references USUARIO(ID)
);
INSERT INTO CONTEUDO(TEXTO,ID_USER)VALUES('OK',1);
SELECT * FROM USUARIO LEFT JOIN CONTEUDO ON USUARIO.ID=CONTEUDO.ID_USER 
WHERE LOGIN='VICTOR' AND SENHA = '123';
;
