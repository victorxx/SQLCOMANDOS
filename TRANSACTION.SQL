CREATE TABLE IF NOT EXISTS DADO(
ID INT PRIMARY KEY auto_increment,
NOME TEXT,
QUANTIDADE INT
);
INSERT INTO DADO(NOME,QUANTIDADE)VALUES('XBOX ONE',33);
START TRANSACTION;
INSERT INTO DADO(NOME,QUANTIDADE)VALUES('XBOX CLASSICO',33);
COMMIT;
SELECT * FROM DADO;
START TRANSACTION;
INSERT INTO DADO(NOME,QUANTIDADE)VALUES('XBOX 360',300);
ROLLBACK;
