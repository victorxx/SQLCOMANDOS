CREATE TABLE IF NOT EXISTS CONTAS(
ID INT PRIMARY KEY auto_increment,
SALDO INT
);
INSERT INTO CONTAS(SALDO)
VALUES(1000),(20),(300);
START TRANSACTION;
SELECT * FROM CONTAS WHERE ID IN(1,2) FOR UPDATE;
COMMIT;
