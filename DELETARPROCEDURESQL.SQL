CREATE TABLE IF NOT EXISTS PRODUTO(
ID INT PRIMARY KEY auto_increment,
NOME VARCHAR(300),
QUANTIDADE INT
);

DROP PROCEDURE SUB;
DELIMITER //
CREATE PROCEDURE SUB(IN P INT)
BEGIN
DECLARE X INT DEFAULT 0;
SELECT QUANTIDADE INTO X FROM PRODUTO WHERE ID=1;
IF X - P < 0 THEN 
      SELECT'NÃO TEMOS ESSE SALDO' AS MENSAGEM;
END IF;
END//
