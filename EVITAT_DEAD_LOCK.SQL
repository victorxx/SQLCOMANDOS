CREATE TABLE IF NOT EXISTS DADO(
ID INT primary KEY auto_increment,
SALDO INT
);
SELECT * FROM DADO;
INSERT INTO DADO(SALDO)VALUES(1000),(2000),(30);
START TRANSACTION;
SELECT * FROM DADO WHERE ID=1 FOR UPDATE;
UPDATE DADO SET SALDO=SALDO+100 WHERE ID=1;
COMMIT;
